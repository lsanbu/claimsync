# =============================================================================
# ClaimSync — Python Dependencies
# =============================================================================
# Project      : ClaimSync (Kaaryaa Intelligence LLP)
# Script       : downloadTxnFilesv8g.py
# Generated    : March 2026
# Python       : 3.12
#
# Sections:
#   1. Current production dependencies (v8g)
#   2. Phase 0 addition — httpx (replaces curl subprocess)
#   3. Phase 1+ placeholders — Azure stack (commented out until needed)
#
# Standard library modules used (NOT listed here — built into Python):
#   base64, configparser, datetime, errno, glob, os, pathlib,
#   platform, re, shutil, subprocess, sys, time, zipfile
# =============================================================================


# -----------------------------------------------------------------------------
# 1. CURRENT PRODUCTION DEPENDENCIES (v8g baseline)
# -----------------------------------------------------------------------------

# Symmetric encryption for INI credential fields (userid, password, licence)
cryptocode==0.1

# TOTP / 2FA — admin authentication gate (askitsolsauthenticate)
pyotp==2.9.0

# QR code generation for TOTP onboarding (new client setup flow)
qrcode==8.2

# Human-friendly datetime handling — interval windowing, log timestamps
arrow==1.4.0


# -----------------------------------------------------------------------------
# 2. PHASE 0 — httpx (P0-T04 / P0-T05: replaces curl subprocess calls)
# -----------------------------------------------------------------------------

# Modern async-capable HTTP client — replaces curl subprocess for SOAP calls
# Used in: build_and_execute_search_request(), DownloadTransactionFile()
httpx==0.28.1


# -----------------------------------------------------------------------------
# 3. PHASE 1+ — AZURE STACK (uncomment as each phase is implemented)
# -----------------------------------------------------------------------------

# Phase 1 — Azure Blob Storage (replaces local folder writes)
# azure-storage-blob==12.24.0

# Phase 1 — Azure Key Vault (replaces INI credential fields)
# azure-keyvault-secrets==4.9.0

# Phase 1 — Azure Identity (shared auth for Key Vault + Blob)
# azure-identity==1.21.0

# Phase 1 — PostgreSQL driver (replaces INI config with DB-backed ConfigProvider)
# psycopg2-binary==2.9.10

# Phase 3 — FastAPI (tenant dashboard API layer)
# fastapi==0.115.0

# Phase 3 — Uvicorn (ASGI server for FastAPI)
# uvicorn[standard]==0.34.0
bcrypt==4.1.2
PyJWT==2.8.0
